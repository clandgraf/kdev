Kernel:

- refactor fb.c/fb.h: 
  - separate vga-specific stuff from interface
  - rename to something more specific
- implement i/o ports
- implement gdt and idt


LibC:

- printf: conversion of negative unsigned numbers.


Project Structure:

- Build-System:
  - move src Makefiles up one level into project Makefiles somehow.
  - move more stuff into config.mk
  - use less relative filepaths

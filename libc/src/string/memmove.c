#include <string.h>

void * memmove(void * dest, void * src, size_t n)
{
    void * dest_ = dest;

    // TODO implement memmove
    // if dst > src && dst < src + n
    //   copy from behind
    // else
    //   call memset

    return dest;
}
